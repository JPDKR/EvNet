@model EvNet.Data.Clientes

@{
    ViewBag.Title = "Nuevo Cliente";
}

<div class="d-flex align-items-center gap-2 page-title mt-3">
    <i class="bi bi-person-plus-fill fs-4 text-success"></i>
    <h2 class="mb-0 fs-4">Nuevo Cliente</h2>
</div>

<div class="row">
    <div class="col-md-7 col-lg-6">
        @if (ViewBag.ErrorLog == true)
        {
            <div class="alert alert-danger alert-dismissible" role="alert">
                <i class="bi bi-exclamation-circle-fill me-2"></i> Ocurrió un error al guardar el cliente.
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Cerrar"></button>
            </div>
        }

        @using (Html.BeginForm("Alta", "Cliente", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true, "", new { @class = "text-danger small" })

            <div class="card">
                <div class="card-header">
                    <i class="bi bi-person-vcard me-2"></i>Datos del Cliente
                </div>
                <div class="card-body p-4">
                    <div class="row g-3">
                        <div class="col-sm-6">
                            <div class="mb-3">
                                @Html.LabelFor(a => a.Nombre, new { @class = "form-label" })
                                @Html.TextBoxFor(a => a.Nombre, new { @class = "form-control", placeholder = "Nombre" })
                                @Html.ValidationMessageFor(a => a.Nombre, "", new { @class = "text-danger small" })
                            </div>
                        </div>
                        <div class="col-sm-6">
                            <div class="mb-3">
                                @Html.LabelFor(a => a.Apellido, new { @class = "form-label" })
                                @Html.TextBoxFor(a => a.Apellido, new { @class = "form-control", placeholder = "Apellido" })
                                @Html.ValidationMessageFor(a => a.Apellido, "", new { @class = "text-danger small" })
                            </div>
                        </div>
                    </div>
                    <div class="mb-3">
                        @Html.LabelFor(a => a.Email, new { @class = "form-label" })
                        <div class="input-group">
                            <span class="input-group-text"><i class="bi bi-envelope"></i></span>
                            @Html.TextBoxFor(a => a.Email, new { @class = "form-control", placeholder = "correo@ejemplo.com" })
                        </div>
                        @Html.ValidationMessageFor(a => a.Email, "", new { @class = "text-danger small" })
                    </div>
                    <div class="mb-3">
                        @Html.LabelFor(a => a.Domicilio, new { @class = "form-label" })
                        <div class="input-group">
                            <span class="input-group-text"><i class="bi bi-geo-alt"></i></span>
                            @Html.TextBoxFor(a => a.Domicilio, new { @class = "form-control", placeholder = "Dirección" })
                        </div>
                        @Html.ValidationMessageFor(a => a.Domicilio, "", new { @class = "text-danger small" })
                    </div>
                    <div class="mb-3">
                        @Html.LabelFor(a => a.Password, new { @class = "form-label" })
                        <div class="input-group">
                            <span class="input-group-text"><i class="bi bi-lock"></i></span>
                            @Html.PasswordFor(a => a.Password, new { @class = "form-control", placeholder = "Contraseña" })
                        </div>
                        @Html.ValidationMessageFor(a => a.Password, "", new { @class = "text-danger small" })
                    </div>
                    <div class="mb-3">
                        @Html.LabelFor(a => a.Ciudades, "Ciudad", new { @class = "form-label" })
                        <div class="input-group">
                            <span class="input-group-text"><i class="bi bi-building"></i></span>
                            @Html.DropDownListFor(a => a.IdCiudad, new SelectList(ViewBag.Ciudades, "Id", "Nombre"), "-- Seleccioná una ciudad --", new { @class = "form-select" })
                        </div>
                        @Html.ValidationMessageFor(a => a.Ciudades, "", new { @class = "text-danger small" })
                    </div>
                    <div class="form-check form-switch mt-3">
                        @Html.CheckBoxFor(a => a.Habilitado, new { @class = "form-check-input", role = "switch" })
                        <label class="form-check-label" for="Habilitado">Cliente habilitado</label>
                    </div>
                </div>
                <div class="card-footer d-flex gap-2">
                    <button type="submit" class="btn btn-success">
                        <i class="bi bi-check-lg me-1"></i> Guardar Cliente
                    </button>
                    @Html.ActionLink("Cancelar", "Index", null, new { @class = "btn btn-outline-secondary" })
                </div>
            </div>
        }
    </div>
</div>
